import React from 'react';
import UserIncard from '../UserIncard';

const Child = (props) => {
    return (
        <div>
            <h2>Child</h2>
            <UserIncard />
        </div>
    );
}

export default Child;
